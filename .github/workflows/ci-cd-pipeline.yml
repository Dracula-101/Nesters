name: Nesters Release Build Workflow

on:
  push:
    branches:
      - prod
      
permissions:
  contents: write

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v3
        with:
          ref: ${{ github.event.inputs.branch }}

      - name: Setup JAVA 17
        uses: actions/setup-java@v3
        with:
          distribution: 'temurin'
          java-version: '17'

      - uses: subosito/flutter-action@v2
        with:
          flutter-version: "3.16.4"
          channel: "stable"

      - name: Install dependencies
        run: flutter pub get

      - name: Build Object Box
        run: flutter pub run build_runner build --delete-conflicting-outputs
      
      - name: Store Version
        run: |
          # get version from pubspec.yaml
          version=$(grep "version: " pubspec.yaml | cut -d' ' -f2)
          echo "version=$version" >> $GITHUB_ENV

      
      - name: Setup Keystore
        run: |
          echo ${{ secrets.BASE64_ENCODE_GOOGLE_SERVICES }} | base64 -d > android/app/google-services.json
          echo ${{ secrets.BASE64_ENCODED_KEY_PROPERTIES }} | base64 -d > android/key.properties
          echo ${{ secrets.BASE64_ENCODED_KEYSTORE }} | base64 -d > keystores/release-keystore.jks
          store_password=$(grep "storePassword" android/key.properties | cut -d'=' -f2 | tr -d '\r')
          key_password=$(grep "keyPassword" android/key.properties | cut -d'=' -f2 | tr -d '\r')
          key_alias=$(grep "keyAlias" android/key.properties | cut -d'=' -f2 | tr -d '\r')
          keytool -list -v -keystore keystores/release-keystore.jks -storepass $store_password -keypass $key_password -alias $key_alias


      - name: Build Nesters Release
        run: |
          echo "Building Nesters release - Version: ${{ env.version }}"
          flutter build apk --release

      - name: Upload Release
        uses: actions/upload-artifact@v3
        with:
          name: Nesters_v${{ env.version }}.apk
          path: build/app/outputs/flutter-apk/app-release.apk
        
      - name: Create GitHub Release
        uses: actions/create-release@v1
        with:
          tag_name: v${{ env.version }}
          release_name: Nesters v${{ env.version }}
          body: "Release version ${{ env.version }} of Nesters."
          draft: false
          prerelease: false
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      - name: Upload APK to Release
        uses: actions/upload-release-asset@v1
        with:
          upload_url: ${{ steps.create_release.outputs.upload_url }}
          asset_path: build/app/outputs/flutter-apk/app-release.apk
          asset_name: Nesters_v${{ env.version }}.apk
          asset_content_type: application/vnd.android.package-archive
          
      - name: Cleanup
        run: |
          rm android/app/google-services.json
          rm android/key.properties
          rm keystores/release-keystore.jks
